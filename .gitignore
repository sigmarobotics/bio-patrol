# Claude Code
CLAUDE.md

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
# *.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# UV
#   uv.lock is committed for reproducible builds


# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Environments
.env.local
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Generated by the gRPC Python protocol compiler plugin 
kachaka_api_pb2*.*

# Kachaka export map
*.kmap

# Runtime data
data/*.db
data/maps/
data/config/*.json
data/config/patrol_presets/
logs/
docs/plans/
docs/superpowers/

# Sigma session context (local state, not committed)
.sigma/

# Site-specific deployment data (maps, locations, network scans — local only)
site/

# Partner TLS credentials (certificates + private keys — never commit)
wisleep-key/

# Local git worktrees (isolated dev workspaces, not committed)
.worktrees/

# Auto-generated tests from /simulate skill (must be reviewed before committing)
tests/generated/

# Playwright MCP browser session artifacts
.playwright-mcp/
src/data/

# Playwright E2E artifacts
tests/e2e/node_modules/
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/e2e/package-lock.json
tests/e2e/screenshots/
test-results/
